{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "private": true,
  "workspaces": [
    "services/**",
    "common",
    "config",
    "gateway"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint './**/src/**' --fix; prettier './**/src/**' --write",
    "start:all": "ts-node index.ts",
    "build": "tsc --build --force"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@hex-labs/eslint-config": "^1.2.3",
    "@hex-labs/prettier-config": "^1.2.0",
    "concurrently": "^7.0.0",
    "eslint": "^8.9.0",
    "lint-staged": "^12.3.4",
    "prettier": "^2.5.1",
    "ts-node": "^10.4.0"
  },
  "dependencies": {
    "typescript": "^4.5.5"
  },
  "eslintConfig": {
    "extends": "@hex-labs/eslint-config"
  },
  "prettier": "@hex-labs/prettier-config",
  "lint-staged": {
    "*.{js,ts,tsx,json,yml,yaml,css,html,md}": [
      "prettier --write"
    ]
  }
}
