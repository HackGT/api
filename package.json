{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "workspaces": [
    "services/**",
    "common",
    "config",
    "gateway"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:all": "concurrently \"cd gateway && yarn start \" \"cd services/event && yarn start \" \"cd services/profile && yarn start \""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.0.0",
    "ts-node": "^10.4.0"
  }
}
